import { Flex, Input } from '@chakra-ui/react'

const TopNav = () => {
    return (
        <Flex padding={3}>
            <Input placeholder='Search..' size='lg' />
        </Flex>
    )
}

export default TopNav
